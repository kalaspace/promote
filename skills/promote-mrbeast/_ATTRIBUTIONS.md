# Attribution — promote-mrbeast

**Nom d'origine** : `mrbeast-perspective`
**Source** : `/home/kala/.agents/skills/mrbeast-perspective`
**Date d'import** : 2026-04-28
**Renommé en** : `promote-mrbeast` (préfixe `promote-` pour éviter les collisions de namespace)
**Frontmatter `name:` modifié** : oui (de `mrbeast-perspective` à `promote-mrbeast`)

## Description

Distillation locale via huashu-nuwa du système de création de contenu de MrBeast (manuel interne 36 pages + 6 podcasts deep). Auteur de la distillation : François Neumann.

## Modifications par rapport à l'original

- Champ `name:` du frontmatter renommé.
- Audit d'autonomie (Phase 0.A.5) à venir : tout chemin absolu, référence externe ou invocation de skill global est patché in-place pour rendre le skill auto-suffisant dans `promote/`.

## Snapshot d'origine

Conservé dans `skills-source/from-local/mrbeast-perspective/` pour traçabilité historique. Cette copie n'est PAS la source de vérité — la version dans `.claude/skills/promote-mrbeast/` l'est.

## Licence

À vérifier avant redistribution publique. Si le SKILL.md d'origine ou le repo source ne précise rien, considérer les droits comme réservés à l'auteur de la distillation.
